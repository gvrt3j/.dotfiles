#!/bin/sh

. $XDG_RUNTIME_DIR/xcolors

command="dmenu"

[ "$1" = "run" ] && command="dmenu_run"

"${command}"\
	-nb "${xres_background}"\
	-nf "${xres_foreground}"\
	-sb "${xres_foreground}"\
	-sf "${xres_background}"
