#!/bin/sh -x

. $XDG_RUNTIME_DIR/xenv

tmp=$(mktemp --suffix=.mkv)
sleep 2
(sleep 0.3; notify-send "Recording started!") &
ffmpeg -thread_queue_size 128 -f x11grab -r 30 -s ${SCREEN_X}x${SCREEN_Y} -i ${DISPLAY}.0 -vcodec libx264 -preset ultrafast -threads 0 -y "$tmp"
wait
echo "$tmp"
